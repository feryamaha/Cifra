/**
 * Tipos da matriz cromática de 12 semitons.
 * PitchClass = inteiro 0..11, onde C = 0, C# = 1, ..., B = 11.
 * Toda a matemática do site se apoia nessa representação canônica.
 */
export type PitchClass = number;
